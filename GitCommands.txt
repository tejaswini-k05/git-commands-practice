Learning Important Git Commands:
1. git config --global color.ui false
Turns off all the colored text in git

2. Commands to check the current repo name 
$ git branch -r
$ git ls-remote --heads
$ git ls-remote
$ git show-branch -r

3. 
